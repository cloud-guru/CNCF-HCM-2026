---
# Test Normal OCI Image Pull with vLLM image (baseline)
# Image: harbor.example.com/myproject/vllm-openai:v0.12.0
# Requires: GPU node (lab4), overlayfs restored (install-normal.yml)
#
# Usage: ansible-playbook -i hosts.ini test-normal-vllm.yml

- name: Cleanup before testing
  hosts: master
  become: yes
  become_user: ubuntu
  vars:
    test_pod_name: "test-normal-vllm"
  tasks:
    - name: Delete existing test pod
      shell: kubectl delete pod {{ test_pod_name }} --ignore-not-found=true
      changed_when: false

    - name: Wait for pod deletion
      pause:
        seconds: 5

- name: Purge image cache on worker nodes
  hosts: workers
  become: yes
  vars:
    test_image: "harbor.example.com/myproject/vllm-openai:v0.12.0"
  tasks:
    - name: List images to find test image
      shell: ctr -n k8s.io images ls | grep "{{ test_image }}" || true
      register: image_check
      changed_when: false

    - name: Remove test image from containerd
      shell: ctr -n k8s.io images rm "{{ test_image }}"
      when: image_check.stdout != ""
      ignore_errors: yes

    - name: Remove all overlayfs snapshots
      shell: |
        ctr -n k8s.io snapshots --snapshotter overlayfs ls | tail -n +2 | awk '{print $1}' | while read snap; do
          if [ -n "$snap" ]; then
            ctr -n k8s.io snapshots --snapshotter overlayfs rm "$snap" 2>/dev/null || true
          fi
        done
      ignore_errors: yes

    - name: Restart containerd to clear metadata cache
      systemd:
        name: containerd
        state: restarted

    - name: Wait for containerd to be ready
      pause:
        seconds: 5

    - name: Display cleanup status
      debug:
        msg: "{{ inventory_hostname }}: cleanup done"

- name: Test normal OCI image pull with vLLM
  hosts: master
  become: yes
  become_user: ubuntu
  vars:
    test_image: "harbor.example.com/myproject/vllm-openai:v0.12.0"
    image_pull_secret: "harbor-creds"
    test_pod_name: "test-normal-vllm"
    log_dir: "./normal-vllm-test-{{ ansible_date_time.epoch }}"
  tasks:
    - name: Create log directory
      file:
        path: "{{ log_dir }}"
        state: directory
      delegate_to: localhost
      become: no

    - name: Export log_dir for later plays
      set_fact:
        normal_vllm_log_dir: "{{ log_dir }}"

    - name: Wait for Cilium to be ready after containerd restart
      pause:
        seconds: 60

    - name: Measure pod startup time with normal OCI pull
      shell: |
        START=$(date +%s.%N)
        kubectl run {{ test_pod_name }} \
          --image={{ test_image }} \
          --overrides='{
            "spec": {
              "nodeName": "worker-4",
              "imagePullSecrets": [{"name": "{{ image_pull_secret }}"}],
              "volumes": [{"name": "models", "persistentVolumeClaim": {"claimName": "llama-models-pvc"}}],
              "containers": [{
                "name": "{{ test_pod_name }}",
                "image": "{{ test_image }}",
                "command": ["vllm", "serve", "/models/meta-llama/Meta-Llama-3-8B", "--host=0.0.0.0", "--port=8000", "--served-model-name", "meta-llama/Meta-Llama-3-8B", "--load-format", "runai_streamer", "--disable-log-requests"],
                "volumeMounts": [{"name": "models", "mountPath": "/models"}],
                "resources": {
                  "limits": {"nvidia.com/gpu": "1"},
                  "requests": {"nvidia.com/gpu": "1"}
                },
                "readinessProbe": {
                  "httpGet": {
                    "path": "/health",
                    "port": 8000
                  },
                  "initialDelaySeconds": 30,
                  "periodSeconds": 10,
                  "timeoutSeconds": 5,
                  "successThreshold": 1,
                  "failureThreshold": 60
                }
              }]
            }
          }'

        if kubectl wait --for=condition=Ready pod/{{ test_pod_name }} --timeout=600s; then
          END=$(date +%s.%N)
          DURATION=$(awk "BEGIN {printf \"%.2f\", $END - $START}")
          echo "=== TIMING ==="
          echo "Duration: ${DURATION} seconds"
          echo ""
          echo "=== POD EVENTS ==="
          kubectl describe pod {{ test_pod_name }} | grep -A 20 "Events:"
          exit 0
        else
          echo "Pod failed to start within 600 seconds"
          kubectl describe pod {{ test_pod_name }} | tail -30
          exit 1
        fi
      register: startup_time
      async: 700
      poll: 15
      ignore_errors: yes

    - name: Get node where pod is running
      shell: kubectl get pod {{ test_pod_name }} -o jsonpath='{.spec.nodeName}'
      register: pod_node
      changed_when: false
      ignore_errors: yes

    - name: Save test results
      copy:
        content: |
          ========================================
          Normal OCI vLLM Pull Test Results
          ========================================
          Timestamp: {{ ansible_date_time.iso8601 }}
          Image: {{ test_image }}
          Pod: {{ test_pod_name }}
          Node: {{ pod_node.stdout | default('N/A') }}

          ========================================
          Startup Time Measurement
          ========================================
          {{ startup_time.stdout }}

          ========================================
          What This Measures
          ========================================
          • Image pull (Traditional OCI - downloads ENTIRE ~8.4GB image before starting)
          • Container creation
          • vLLM model loading
          • Readiness probe success (HTTP /health:8000)

          Status: {{ 'PASSED' if startup_time.rc == 0 else 'FAILED' }}
        dest: "{{ log_dir }}/test-results.log"
      delegate_to: localhost
      become: no

    - name: Display timing results
      debug:
        msg:
          - "========================================"
          - "Normal OCI vLLM Pull Time"
          - "========================================"
          - "{{ startup_time.stdout_lines }}"
          - "{{ 'PASSED' if startup_time.rc == 0 else 'FAILED' }}"
          - "Logs: {{ log_dir }}/"

- name: Test vLLM inference endpoint (baseline)
  hosts: master
  become: yes
  become_user: ubuntu
  vars:
    test_pod_name: "test-normal-vllm"
  tasks:
    - name: Send Hello prompt to vLLM chat endpoint
      shell: |
        START=$(date +%s.%N)
        OUTPUT=$(kubectl exec {{ test_pod_name }} -- curl -s -X POST http://localhost:8000/v1/completions \
          -H "Content-Type: application/json" \
          -d '{"model": "meta-llama/Meta-Llama-3-8B", "prompt": "Hello", "max_tokens": 50}')
        END=$(date +%s.%N)
        DURATION=$(awk "BEGIN {printf \"%.2f\", $END - $START}")
        echo "$OUTPUT"
        echo "Inference time: ${DURATION} seconds"
      register: inference_test
      ignore_errors: yes

    - name: Save inference test to log
      copy:
        content: |
          ========================================
          vLLM Inference Endpoint Test (Normal OCI)
          ========================================
          {{ inference_test.stdout }}

          Status: {{ 'PASSED' if inference_test.rc == 0 else 'FAILED' }}

          Baseline: full image downloaded before any serving starts.
        dest: "{{ hostvars[groups['master'][0]]['normal_vllm_log_dir'] }}/inference-test.log"
      delegate_to: localhost
      become: no

    - name: Display inference test result
      debug:
        msg:
          - "vLLM /v1/models: {{ 'PASSED' if inference_test.rc == 0 else 'FAILED' }}"
          - "{{ inference_test.stdout_lines }}"

- name: Verify worker nodes
  hosts: workers
  become: yes
  vars:
    test_image: "harbor.example.com/myproject/vllm-openai:v0.12.0"
    log_dir: "{{ hostvars[groups['master'][0]]['normal_vllm_log_dir'] }}"
  tasks:
    - name: Check if image was pulled
      shell: ctr -n k8s.io images ls | grep "{{ test_image }}" || echo "Image not found"
      register: image_status
      changed_when: false

    - name: Get image size from containerd
      shell: ctr -n k8s.io images ls | grep "{{ test_image }}" | awk '{print $7, $8}'
      register: image_size_ctr
      changed_when: false
      ignore_errors: yes

    - name: Save worker node status
      blockinfile:
        path: "{{ log_dir }}/worker-image-stats.log"
        marker: "# {mark} {{ inventory_hostname }}"
        create: yes
        block: |
          Worker: {{ inventory_hostname }}
          Image Status: {{ 'Pulled' if image_status.stdout != 'Image not found' else 'Not found' }}
          Image Size: {{ image_size_ctr.stdout | default('N/A') }}
      delegate_to: localhost
      become: no

    - name: Display worker node status
      debug:
        msg:
          - "{{ inventory_hostname }}: {{ 'Pulled' if image_status.stdout != 'Image not found' else 'Not found' }}, size={{ image_size_ctr.stdout | default('N/A') }}"
